- [AForce build-lock reversal](aforce-build-lock.md) — "Build once, Evaluate everything, Release later — no hiding"; gating decisions must surface gates to evaluators, never hide modules.
- [AForce Water-First wording lock](aforce-water-first.md) — coach copy must start with `HYDRATE NOW` / `Start with water`; products never lead, scans/recommendations never change score; launch i18n = en/es/fr/de/pt/it only.
- [AForce Score-Protection illumination](aforce-score-protection-illumination.md) — today-scoped "lights up" UI must derive from today's behavior, never a persistent field with a non-zero server default.
- [aforce-os icon map discipline](aforce-icons.md) — Icon name strings must exist in `theme/icons.ts` ICON_MAP; lucide name ≠ map key (e.g. `refresh-cw` not `refresh-ccw`, no `link`).
- [AForce wordmark](aforce-wordmark.md) — pitch deck brand mark is the official AFORCE logo *artwork* via the shared `Wordmark` image component; never typeset the logo as text.
- [Expo Router app/ routing + false crash reports](aforce-expo-router-routes.md) — every app/ file (except _layout/+files) is a route; underscore doesn't exclude → "missing default export"; keep helpers in components/; white web screenshots are capture artifacts.
- [aforce-pitch adding a slide](aforce-pitch-add-slide.md) — slide bodies auto-load from the manifest, but TOTAL_SLIDES + SECTIONS in SlideChrome.tsx are hardcoded and must be updated by hand or the footer count/section is wrong.
- [aforce-pitch lender disclosures](aforce-pitch-disclosures.md) — credit-memo edits: don't stack a 2nd disclaimer next to ProjectionDisclaimer (replace it); Use-of-Funds is value-sorted, confirm before reordering.
- [aforce-pitch design language](aforce-pitch-design.md) — grey-paper editorial deck (light slides share slide-7 grey-wall tone via SlideFrame PAPER_BG); Inter for ALL type (a serif/Bodoni magazine display was tried and user rejected it — don't reintroduce serif display without an explicit ask); three-color brand system red=acquisition / blue #2f5bff=OS·retention / black=body.
- [AForce can label text](aforce-can-labels.md) — can PNG label copy is baked into the 4000px images; fix typos with font-free pixel surgery (borrow a real glyph + shift), never AI re-render (keeps typo + downsamples); first word is lighter gray than the rest.
- [AForce dependency hardening](aforce-dependency-hardening.md) — Clerk CVE fix is exact peer-pinned (not caret) floor versions; never bump react past Expo's 19.1.0; restart expo after any install; uuid CVE accepted (build-only via xcode).
- [AForce pre-existing failing tests](aforce-stale-tests.md) — slice.units.test.ts has 4 stale failures (test predates `height` unit pref in utils/units.ts); not a regression, out of scope for refactors.
- [aforce-os white screen = stale Metro cache](aforce-metro-stale-cache.md) — crash-before-Clerk / blank web after a big refactor is usually a stale incremental bundle; clear caches + full rebuild, don't chase a code bug first.
- [Fast Refresh "Invalid hook call" false alarm](aforce-fast-refresh-hooks.md) — adding/removing a hook mid-session throws "Invalid hook call" (often blamed on a child like ScoreDrivenBody); it's a hot-reload artifact — restart the workflow before debugging.
- [AForce first-run gating](aforce-onboarding-gate.md) — welcome lobby removed; ONE flag hasCompletedOnboarding via pure firstRunRoute; SplashGate + index.tsx must read the same key; never mark onboarding done before the wizard finishes.
- [AForce explainability drivers](aforce-explainability.md) — plain 4-driver score "why" (Water/Sleep/Heat/Recovery) rolls up the SAME contributions; one jargon-free sentence each; scoring tests must live in utils/__tests__.
- [AForce Daily Wins engine](aforce-daily-wins.md) — positive-only, one-line reinforcement on home; pure helper shows top win from REAL signals, renders nothing (never a downer) when none; retention not gamification.
- [AForce Analytics Layer](aforce-analytics.md) — INTERNAL no-UI analytics; keep math in the pure metrics module (service/hooks untestable), persisted appends MUST go through the serialized write queue, signals never fabricated.
- [AForce adaptive reminders](aforce-adaptive-reminders.md) — no recurring reminder generator exists; "smart reminders" = a pure policy that gates the fixed welcome cadence (levels + rules + hard fatigue guardrails).
- [AForce Time-To-Log KPI](aforce-time-to-log.md) — friction metric measured surface-active→tap (re-anchor on focus), recorded at tap not after write; derive fn stays pure; additive home row, no flag.
- [AForce Smart Modes + Friction Score](aforce-smart-modes.md) — exactly 4 pure modes; only reminderMultiplier (default-1 no-op) is live, target multiplier is guidance-only; travel dormant; Friction's time-to-first-log is onboarding→first-log latency, NOT per-log median.
- [AForce Impact Engine & Verification Layer](aforce-impact-verification.md) — pure no-UI engines; Impact only MEASURES (Score-Protection), Verification puts Phantom above wearables with phone as the unconditional floor.
- [Slide-scoped audio + autoplay-unlock](aforce-slide-scoped-audio.md) — audio created/torn-down in a slide's useEffect is auto-scoped (loader mounts one slide); label must be play/pause-event-driven, toggle intent-driven on audio.paused, fallback disarmed via disarmRef to avoid same-click race + stay-armed restart.
- [AForce product image padding vs layout](aforce-product-asset-padding.md) — bg-removed/AI product PNGs come padded (square); `magick -trim +repage` before using in the tight-crop product-stage layout or they sit off-center/undersized.
- [Mockup/preview rendering & screenshot gotchas](mockup-preview-gotchas.md) — SVG gradient needs gradientUnits=userSpaceOnUse or it goes invisible; framer-motion ease needs `as const`; external_url screenshot caches → use app_preview for animations.
- [AForce notification system mockups](aforce-notifications-mockup.md) — premium notification design lives as canvas mockups (sandbox aforce-notifications/), NOT app push; brand-red #FF3B30 + frosted glass; copy Water-First/tone-locked.
- [AForce engine-driven overlay timers](aforce-engine-overlay-timers.md) — overlays reading useEngineSlice() re-render on the ~30s score refresh; hold onFinish in a ref + isolate the finish timer or the timeline tears down mid-play / strands the overlay.
- [AForce Performance Age™](aforce-performance-age.md) — derived display-only age metric; weights 35/30/20/15 (NO command-completion), anchor 75, clamp [-15,+10], floor min(18,age); flag-gated, NO entitlement gate; read-only; trends "collecting" until a daily snapshot series is persisted.
- [AForce hydration personalization home](aforce-demand-engine-personalization.md) — lifestyle/profile tuning goes in the gated demand engine (target-side, additive, capped) + non-scoring chips; live per-event points engine & calculateDailyWaterTarget are dormant — never touch them.
- [AForce branded PDF export tooling](aforce-pdf-export.md) — use wkhtmltopdf + python markdown + file:// Inter @font-face (weasyprint won't install); dark cover page, light body, footer page numbers.
- [AForce Metabolic Readiness](aforce-metabolic-readiness.md) — Athlete-tier display-only estimates; absent OPTIONAL signal ⇒ drop term + renormalize required weights, NEVER default favorably (fabricates score); required absent ⇒ needs-more-data.
- [AForce Home orb accent color](aforce-orb-accent-color.md) — orb renders the 4-band useDisplayedAccent()/scoreBand color, NOT 5-band getStatusColor; match the orb via useDisplayedAccent (fallback accentForScore), never mix the two palettes.
