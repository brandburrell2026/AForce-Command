- [AForce build-lock reversal](aforce-build-lock.md) — "Build once, Evaluate everything, Release later — no hiding"; gating decisions must surface gates to evaluators, never hide modules.
- [AForce Water-First wording lock](aforce-water-first.md) — coach copy must start with `HYDRATE NOW` / `Start with water`; products never lead, scans/recommendations never change score; launch i18n = en/es/fr/de/pt/it only.
- [AForce Score-Protection illumination](aforce-score-protection-illumination.md) — today-scoped "lights up" UI must derive from today's behavior, never a persistent field with a non-zero server default.
- [aforce-os icon map discipline](aforce-icons.md) — Icon name strings must exist in `theme/icons.ts` ICON_MAP; lucide name ≠ map key (e.g. `refresh-cw` not `refresh-ccw`, no `link`).
- [AForce wordmark](aforce-wordmark.md) — pitch deck brand mark is the official AFORCE logo *artwork* via the shared `Wordmark` image component; never typeset the logo as text.
- [Expo Router app/ routing + false crash reports](aforce-expo-router-routes.md) — every app/ file (except _layout/+files) is a route; underscore doesn't exclude → "missing default export"; keep helpers in components/; white web screenshots are capture artifacts.
- [aforce-pitch adding a slide](aforce-pitch-add-slide.md) — slide bodies auto-load from the manifest, but TOTAL_SLIDES + SECTIONS in SlideChrome.tsx are hardcoded and must be updated by hand or the footer count/section is wrong.
- [aforce-pitch lender disclosures](aforce-pitch-disclosures.md) — credit-memo edits: don't stack a 2nd disclaimer next to ProjectionDisclaimer (replace it); Use-of-Funds is value-sorted, confirm before reordering.
- [aforce-pitch design language](aforce-pitch-design.md) — grey-paper editorial deck (light slides share slide-7 grey-wall tone via SlideFrame PAPER_BG); Inter for ALL type (a serif/Bodoni magazine display was tried and user rejected it — don't reintroduce serif display without an explicit ask); three-color brand system red=acquisition / blue #2f5bff=OS·retention / black=body.
- [AForce can label text](aforce-can-labels.md) — can PNG label copy is baked into the 4000px images; fix typos with font-free pixel surgery (borrow a real glyph + shift), never AI re-render (keeps typo + downsamples); first word is lighter gray than the rest.
- [AForce dependency hardening](aforce-dependency-hardening.md) — Clerk CVE fix is exact peer-pinned (not caret) floor versions; never bump react past Expo's 19.1.0; restart expo after any install; uuid CVE accepted (build-only via xcode).
- [AForce pre-existing failing tests](aforce-stale-tests.md) — slice.units.test.ts has 4 stale failures (test predates `height` unit pref in utils/units.ts); not a regression, out of scope for refactors.
- [aforce-os white screen = stale Metro cache](aforce-metro-stale-cache.md) — crash-before-Clerk / blank web after a big refactor is usually a stale incremental bundle; clear caches + full rebuild, don't chase a code bug first.
- [Fast Refresh "Invalid hook call" false alarm](aforce-fast-refresh-hooks.md) — adding/removing a hook mid-session throws "Invalid hook call" (often blamed on a child like ScoreDrivenBody); it's a hot-reload artifact — restart the workflow before debugging.
- [AForce first-run gating](aforce-onboarding-gate.md) — welcome lobby removed; ONE flag hasCompletedOnboarding via pure firstRunRoute; SplashGate + index.tsx must read the same key; never mark onboarding done before the wizard finishes.
- [AForce explainability drivers](aforce-explainability.md) — plain 4-driver score "why" (Water/Sleep/Heat/Recovery) rolls up the SAME contributions; one jargon-free sentence each; scoring tests must live in utils/__tests__.
- [AForce Daily Wins engine](aforce-daily-wins.md) — positive-only, one-line reinforcement on home; pure helper shows top win from REAL signals, renders nothing (never a downer) when none; retention not gamification.
- [AForce Analytics Layer](aforce-analytics.md) — INTERNAL no-UI analytics; keep math in the pure metrics module (service/hooks untestable), persisted appends MUST go through the serialized write queue, signals never fabricated.
- [AForce adaptive reminders](aforce-adaptive-reminders.md) — no recurring reminder generator exists; "smart reminders" = a pure policy that gates the fixed welcome cadence (levels + rules + hard fatigue guardrails).
- [AForce Time-To-Log KPI](aforce-time-to-log.md) — friction metric measured surface-active→tap (re-anchor on focus), recorded at tap not after write; derive fn stays pure; additive home row, no flag.
- [AForce Smart Modes + Friction Score](aforce-smart-modes.md) — exactly 4 pure modes; only reminderMultiplier (default-1 no-op) is live, target multiplier is guidance-only; travel dormant; Friction's time-to-first-log is onboarding→first-log latency, NOT per-log median.
- [AForce Impact Engine & Verification Layer](aforce-impact-verification.md) — pure no-UI engines; Impact only MEASURES (Score-Protection), Verification puts Phantom above wearables with phone as the unconditional floor.
- [Slide-scoped audio + autoplay-unlock](aforce-slide-scoped-audio.md) — audio created/torn-down in a slide's useEffect is auto-scoped (loader mounts one slide); label must be play/pause-event-driven, toggle intent-driven on audio.paused, fallback disarmed via disarmRef to avoid same-click race + stay-armed restart.
- [AForce product image padding vs layout](aforce-product-asset-padding.md) — bg-removed/AI product PNGs come padded (square); `magick -trim +repage` before using in the tight-crop product-stage layout or they sit off-center/undersized.
- [Mockup/preview rendering & screenshot gotchas](mockup-preview-gotchas.md) — SVG gradient needs gradientUnits=userSpaceOnUse or it goes invisible; framer-motion ease needs `as const`; external_url screenshot caches → use app_preview for animations.
- [AForce notification system mockups](aforce-notifications-mockup.md) — premium notification design lives as canvas mockups (sandbox aforce-notifications/), NOT app push; brand-red #FF3B30 + frosted glass; copy Water-First/tone-locked.
- [AForce engine-driven overlay timers](aforce-engine-overlay-timers.md) — overlays reading useEngineSlice() re-render on the ~30s score refresh; hold onFinish in a ref + isolate the finish timer or the timeline tears down mid-play / strands the overlay.
- [AForce Location Intelligence surfacing](aforce-location-intelligence.md) — ambient note = LocationInsightBanner; travel = Smart Modes travel mode (never double-surface); travel_protocol key = i18n contract; Smart Modes guidance still English-only.
- [vitest RN transform failures](aforce-vitest-rn-transform.md) — test files that import react-native fail at LOAD (`import typeof` RollupError), not as assertion failures; "N files failed / M tests passed, 0 failed" = env noise. Keep pure logic RN-free + tested under the included __tests__ globs.
- [AForce Fuel vs Store naming](aforce-fuel-store-naming.md) — commerce surface is branded "Fuel" (guided, not sold) in UI, but route `/store`, i18n KEY `tabs.store`, and `*Store*`/`useCartStore` ids stay `store`; rename labels only, never identifiers.
- [AForce persistence guarantees & offline gap](aforce-persistence-guarantees.md) — server-is-truth so restart/force-close/update survival = server round-trip + Clerk tokenCache; NO offline outbox (airplane-mode intake logs are lost); state.history reseeds from mock; PA derived; dev curl via requireAuth DEFAULT_USER_ID fallback.
- [AForce performance-requirements audit](aforce-performance-requirements.md) — scoring engine runs ON-DEVICE (server only persists client-submitted scores, not authoritative); territory is client-side lazy (no 30-min job); weather+TTS caches are per-process in-memory Maps (not scale-safe); opening mounts immediately but 2s SLO uninstrumented; first command synchronous/no-network.
- [AForce global-support audit](aforce-global-support.md) — units metric+imperial fully cover height/weight/temp (PASS); numbers NOT localized (toFixed+hardcoded, no Intl.NumberFormat) & dates bound to device locale not i18n selection; Arabic wired-but-GATED (HIDDEN_LANGUAGES + placeholder=English JSON + RTL needs relaunch/audit); foldable device-class system exists (narrow≤319/foldOpen 600-839) but no physical Fold test.
- [AForce experience-layer status](aforce-experience-layer.md) — Territory/Circles/Performance-Timeline are real reachable screens (PASS); "Performance Mode" = dormant AthleteModeCard + voice autopilot toggle (NO optimization environment); "Competition Mode" = competitionEngine + rankings only (NO event-day protocol flow / no competition-mode route).
- [AForce Cruise vertical gating](aforce-cruise-gating.md) — FeatureGate is a self-serve UPSELL (off-state shows "Activate Demo" that flips the flag), NOT a security boundary; legacy /cruise is consumer-exposed (cruise_mode_enabled defaults true + ungated Modules launcher + no identity gate); use the (hidden)/cruise spec_cruise Redirect fail-closed pattern instead; API cruise routes ungated.
- [AForce Weekly Report anchoring](aforce-weekly-report.md) — full-log signals (streak) must be gated on `!noWeekData` before showing as a "this week" win, or an empty report week fabricates one (Score-Protection); entry chrome stays hardcoded-English by convention.
- [AForce Command Center (founder cockpit)](aforce-command-center.md) — separate founder-gated WEB artifact; own local fetch client (never the consumer api-client); super_admin+FOUNDER_EMAILS gate, cookie auth; pinned @clerk/react@6.4.3 lacks publishableKeyFromHost → use VITE_CLERK_PUBLISHABLE_KEY directly; Product tab = 5 Retention Gates (empty=awaiting, never 0%).
- [AForce QR activation funnel](aforce-qr-activation.md) — now end-to-end (mobile emit → server aggregate → founder /activation panel); deep-link trust = scheme+host-allowlist+exact-path (never substring); conversions need chronology; founder analytics SQL aggregate-only, never SELECT analytics_id/PII.
- [AForce signal-quality layers](aforce-signal-quality-layers.md) — three DISTINCT pure layers: Verification (per-source trust 0..1) vs Signal Hierarchy (source selection) vs Data Confidence (categorical High/Med/Low completeness); don't merge; consumers opt in by calling adapters, never widen output shapes.
- [AForce wearable integration status](aforce-wearable-integration-status.md) — only Apple HealthKit is a real installed native module; Samsung/Garmin/Health-Connect are scaffold/demo only; WHOOP fully coded but DORMANT; `il`=WHOOP server codename; WHOOP OAuth 404s unless 3 WHOOP_* env vars set.
- [aforce-os vitest RN parse failures](aforce-vitest-rn-parse.md) — full-suite vitest: ~13 test FILES fail to LOAD on react-native `import typeof` parse error; pre-existing env, not a regression; run pure tests by path.
- [AForce boot-hydration race](aforce-boot-hydration-race.md) — module-boot `void hydrate()` AsyncStorage pub-sub stores must MERGE-by-id on load, not overwrite, or an early sync write is clobbered; test by recording before awaiting hydrate.
- [Time-based isDue overlays](aforce-engine-overlay-timers.md) — store notifies only on writes, so schedule a snooze re-check ONLY for a future expiry (pure snoozeRevalidationDelay); clear the `activated` latch on close/snooze, never a permanent `dismissed`.
- [AForce Performance Age™](aforce-performance-age.md) — derived display-only age metric; weights 35/30/20/15 (NO command-completion), anchor 75, clamp [-15,+10], floor min(18,age); flag-gated, NO entitlement gate; read-only; daily 7/30-day trends now LIVE via persisted ledger snapshot series (honest accrual, first-of-day locked).
- [AForce hydration personalization home](aforce-demand-engine-personalization.md) — lifestyle/profile tuning goes in the gated demand engine (target-side, additive, capped) + non-scoring chips; live per-event points engine & calculateDailyWaterTarget are dormant — never touch them.
- [AForce branded PDF export tooling](aforce-pdf-export.md) — use wkhtmltopdf + python markdown + file:// Inter @font-face (weasyprint won't install); dark cover page, light body, footer page numbers.
- [AForce Metabolic Readiness](aforce-metabolic-readiness.md) — Athlete-tier display-only estimates; absent OPTIONAL signal ⇒ drop term + renormalize required weights, NEVER default favorably (fabricates score); required absent ⇒ needs-more-data.
- [AForce Home orb accent color](aforce-orb-accent-color.md) — orb renders the 4-band useDisplayedAccent()/scoreBand color, NOT 5-band getStatusColor; match the orb via useDisplayedAccent (fallback accentForScore), never mix the two palettes.
- [AForce Investor Demo Phase 10](aforce-investor-demo.md) — FINAL_SPEC's "gold/lime" + "returns to welcome.tsx" are STALE (brand re-skin = red+WHOOP states; no welcome screen, Modal close reveals app); root vitest globs skip data/ & featureFlags/ pure tests.
- [AForce Performance Statements™](aforce-performance-statements.md) — voice-only once/day coach identity line; data path inert (no daily recovery memory → would fabricate); flag-off-inert needs an OUTER flag gate + NO module-boot hydrate (sibling mounts don't do this); mute=skip w/o marking, re-check AppState+mute inside the speak delay.
- [AForce Intent Capture / coach posture](aforce-intent-capture.md) — coach tone/intensity is COPY-ONLY (posture picks an i18n key, never a score dispatch); neutral toneKey reuses the original closing keys so flag-off is byte-identical; new post-check-in steps go inline in VoiceCheckInOverlay (flag-gated), never a new tab.
- [AForce spec-doc vs code drift](aforce-spec-vs-code-drift.md) — replit.md lags the code; many "net-new" trademarked features (Perf Age / Command Confidence / Perf Memory / Voice Check-In / Command Center / QR Activation / body-model) already shipped+wired; audit CODE before building gaps.
- [AForce score-from-ledger integration](aforce-score-ledger-integration.md) — ledger = score INPUT source (interp A, values unchanged); projection fails closed until a family is losslessly derivable; parity gate stricter than final score (0/100 clamp hides input drift).
- [AForce command-ledger freshness anchoring](aforce-ledger-freshness-anchoring.md) — ledger context snapshots anchor freshness to each signal's SOURCE fetch time (not sync), one clock, flag derived from anchor (fail-closed), one shared helper for hook+parity test.
- [AForce shared body-model input](aforce-body-model.md) — ONE HeightField/WeightField pair is the single source of truth (canonical lbs + int cm via setProfileIdentity); both emit number|null; allowClear only where the field is optional (EditProfile); body edits feed targets/sweat math, never score.
- [AForce Intelligence Core ledger + adapters](aforce-intelligence-core.md) — additive RN-free ledger+adapters over pre-existing engines (signatures green); preserve per-source day-index; adherence NOT wired to confidence; append defers persist past boot-hydrate; clear() generation guard.
- [AForce once-ever analytics emits](aforce-once-ever-emit.md) — burn a once-EVER dedupe flag only AFTER a durable enqueue, never before, or the milestone is lost forever.
