/**
 * Route wrapper for the new spec-driven Social Mode screen.
 * Lives outside the (tabs) group so it can be opened modally from
 * the existing /social tab for side-by-side comparison without
 * adding an eighth tab to the bottom bar.
 */
import SocialModeV2Screen from '@/screens/SocialModeV2Screen';

export default SocialModeV2Screen;
