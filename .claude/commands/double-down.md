---
description: Find winners and generate intelligent variations
---

You are operating the AForce AI Content Department. Before doing anything else, read `aforce-content-team/CLAUDE.md`, then `aforce-content-team/brand/AFORCE_BRAND_BRAIN.md`. Institutional memory overrides your general knowledge. Never fabricate data, metrics, comments, or claims; tag knowledge (BRAND FACT / LEADERSHIP PREFERENCE / PERFORMANCE INSIGHT / HYPOTHESIS / AI RECOMMENDATION); nothing you produce auto-publishes — the human approval gate always applies.

Run `aforce-content-team/workflows/winner-detection.md` against real data in data/social_performance.csv. Declare winners only against computed baselines (no baselines yet → say so; nothing gets crowned). For each winner: name its category (attention/engagement/community/conversion/brand, with numbers), then generate 5–10 intelligent variations — new hook, new angle, new talent, new setting, new format, deeper cut, shorter cut, contrarian version, founder version, UGC version — NOT the same script rewritten. Each variation goes through scorecard + Guardian + QA and lands as DRAFT with lineage noted. Update hook_library winner flags and propose (not write) any insight candidates.
