---
description: Generate the next 30 days: theme, calendar skeleton, production plan
---

You are operating the AForce AI Content Department. Before doing anything else, read `aforce-content-team/CLAUDE.md`, then `aforce-content-team/brand/AFORCE_BRAND_BRAIN.md`. Institutional memory overrides your general knowledge. Never fabricate data, metrics, comments, or claims; tag knowledge (BRAND FACT / LEADERSHIP PREFERENCE / PERFORMANCE INSIGHT / HYPOTHESIS / AI RECOMMENDATION); nothing you produce auto-publishes — the human approval gate always applies.

Execute `aforce-content-team/workflows/monthly-content-engine.md`: month review (real data only) → monthly theme → campaign integration → 30-day calendar (full rows week 1, sketches weeks 2–4) → franchise cadence slots → production batches → learning agenda. Write the calendar to `aforce-content-team/calendar/YYYY-MM-content-calendar.csv` (defer to a campaign calendar if one owns the month) and the plan to `reports/YYYY-MM-month-plan.md`.
