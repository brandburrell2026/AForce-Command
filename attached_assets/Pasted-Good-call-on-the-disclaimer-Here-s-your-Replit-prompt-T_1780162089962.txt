Good call on the disclaimer. Here's your Replit prompt:

---

**This is a targeted fix on slide 13 of the existing AForce 14-slide deck HTML file. Four specific changes only. Do not touch anything else.**

---

**FIX 1 — LTV retention benchmark footnote**

On the LTV stat box, directly beneath the existing subline `$119/mo · 8 mo avg` add a second subline in grey `#999`, italic, 8px, letter-spacing 0.5px:

`Based on comparable subscription wellness brands · conservative benchmark`

---

**FIX 2 — Update October snapshot numbers**

Change the following values in the October snapshot dark box only:

- Active subscribers: change `264` to `144`
- Monthly sub rev: change `$31,416` to `$17,136`
- ARR run rate: change `$377K` to `$205K`

Leave all other October snapshot values exactly as they are:
- Total customers: `1,200` — unchanged
- Repeat rate: `30%` — unchanged
- NPS target: `55+` — unchanged

---

**FIX 3 — Add monthly burn and runway line**

Inside the October snapshot dark box, add a full-width divider line `1px solid rgba(255,255,255,0.08)` beneath the existing 2×3 grid, then add one line beneath it:

Left aligned: `MONTHLY BURN` in grey `#666`, 9px, letter-spacing 2px, small caps
Value: `~$150K` in white, Barlow Condensed Bold, 16px

Center divider `|` in grey

Left aligned: `RUNWAY ON $4M` in grey `#666`, 9px, letter-spacing 2px, small caps
Value: `26 months` in white, Barlow Condensed Bold, 16px

Both pairs sit on one line, separated by a centered grey divider.

---

**FIX 4 — Add disclaimer**

At the very bottom of slide 13, between the content area and the footer bar, add a single centered disclaimer line:

Style: grey `#aaa`, italic, 9px, letter-spacing 0.5px, centered, max-width 900px, margin 0 auto

Text:
`All financial projections are illustrative and forward-looking. Unit economics based on actual vendor quotes from iLabs (March 2026) and Bev-Hub (2026). Subscription retention benchmarks based on comparable wellness brands. Actual results may vary materially.`

---

**DO NOT CHANGE:**
- Any other slide in the deck
- The LTV:CAC ratio — 29.8× stays as is, it's calculated correctly from the original $952 LTV and $32 CAC
- The subscription tier cards — Starter $49 and Full System $119 stay exactly as they are
- The gross margin bars — all five stay exactly as they are
- The stat boxes — CAC $32, LTV $952, Sub Conversion 20%+ stay exactly as they are
- All navigation, transitions, footers, slide numbers

---

**Quality check before saving:**
- LTV box shows two sublines — `$119/mo · 8 mo avg` and the benchmark footnote beneath it
- October snapshot shows `144` subscribers, `$17,136` monthly rev, `$205K` ARR
- Burn/runway line sits cleanly inside the dark box beneath the grid
- Disclaimer sits between content and footer — not inside the footer, not overlapping content
- Nothing else on the slide has changed

---

Paste this directly into Replit. Four fixes only. Everything else stays exactly as it is.