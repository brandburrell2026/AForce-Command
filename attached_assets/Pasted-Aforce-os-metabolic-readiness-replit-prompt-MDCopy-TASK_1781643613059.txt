Aforce os metabolic readiness replit prompt · MDCopyTASK: Add a "Metabolic Readiness" section to AForce OS — gated behind the AForce Athlete tier

You are working inside the existing AForce OS codebase (Expo SDK ~54, React Native 0.81.5, Expo Router 6, Zustand, @tanstack/react-query, Clerk auth, Stripe). Do NOT scaffold a new app. Read the existing patterns first and match them exactly.


NON-NEGOTIABLE CONSTRAINTS (read before writing any code)


SCORE PROTECTION IS SACRED. The existing hydration scoring engine (services/hydrationScoreService.ts, utils/hydrationScore.ts, utils/scoringEngine.ts) and recovery engine (services/recoveryEngine.ts) must NOT be modified. The new feature is a READ-ONLY downstream layer. It imports their outputs; they must NEVER import it. One-directional dependency only. Verify there is no path by which metabolic code can mutate a hydration point, performance band, or recovery score.
PURELY ADDITIVE & REVERSIBLE. Everything ships behind a feature flag. Flipping the flag off must return the app to its exact current state. No changes to the 5 visible tabs, no navigation churn.
NO MEDICAL CLAIMS. These are wellness ESTIMATES derived from existing signals, never clinical measurements. Do NOT build anything glucose- or insulin-related. Every metric must carry a visible "wellness estimate, not a medical measurement" disclaimer in the UI.



WHAT TO BUILD

A. Feature flag (featureFlags/flags.ts)


Add metabolic_readiness_enabled — false in DEFAULT_FLAGS, true in DEMO_ALL_ON_FLAGS.
Add metabolic_glucose_enabled — false everywhere, NO UI wired to it (architecture-ready only).
Follow the exact shape of the existing flags in this file.


B. Entitlement (match the existing inheritance chain in data/subscriptionPlans.ts + useEntitlement.ts)


Add a metabolic_readiness entitlement granted at the AForce Athlete ($19.99) node.
Because plans inherit features up the chain, Athlete, Performance Bundle, and Elite must all resolve to having it; Core and Recovery+ must NOT. Wire it at the Athlete node only and let inheritance handle the rest. Confirm this by tracing the chain.


C. Pure scoring math (utils/metabolicScore.ts — NEW, dependency-free)

Mirror the style of utils/hydrationScore.ts: pure functions, no imports from services, fully unit-testable. Implement two derived metrics on a 0–100 scale with bands matching the existing PEAK/BALANCED/RECOVERING/DEPLETED convention:


computeMuscleReadiness({ hydrationScore, workoutLoad, recoveryCapacity })
computeCognitiveReadiness({ hydrationScore, sleepScore, hrv })


Include clear JSDoc on each function stating inputs, output range, and that outputs are display-only estimates. Handle missing inputs gracefully (return a "needs more data" sentinel, never NaN or a fabricated number). Exact formulas and weights are locked in the next section.

D. Service layer (services/metabolicReadinessService.ts — NEW)


Imports ONLY the read outputs of hydrationScoreService, recoveryEngine, and biometricsAggregator. Never writes back.
Pulls sleep + HRV from the existing HealthKit path (services/healthConnection.ts) — reuse, don't duplicate.
Exposes a single hook-friendly selector returning { muscleReadiness, cognitiveReadiness, hasEnoughData, lastUpdated }.


E. UI surface


Add the section to the Home dashboard (app/(tabs)/index.tsx) as a new card block, OR as a hidden route (href: null) if cleaner — match how scan/sleep are handled. Do not add a visible tab.
Match the WHOOP-Cinematic design system exactly (theme/colors.ts, theme/typography.ts): pure black canvas, big number / small tracked label, soft radial glow, no hard shadows. Use the existing brand red accent SPARINGLY. Inter for numerics, Bebas Neue for the section header.
LOCKED-PREVIEW STATE for non-Athlete users: render the section with real layout but blurred/locked metric values and an "Unlock with AForce Athlete" CTA routing to /subscription. Subscribers see live scores. This is the conversion lever — make the locked state feel intentional and premium, not broken.
Each metric tile shows: the score, its band, a one-line plain-language read, and the wellness-estimate disclaimer.



SCORING WEIGHTS — LOCKED VALUES (use exactly these; do not improvise)

These weights are deliberate. Hydration is the brand's core signal, so it always carries meaningful weight, but it must never be the sole driver — that would make the metric feel like a relabeled hydration score. Recovery and sleep are the differentiators that justify the Athlete tier.

Muscle Readiness = clamp0to100(
    0.40 * hydrationScore
  + 0.40 * recoveryCapacity
  + 0.20 * (100 - normalizedWorkoutFatigue)   // high recent load lowers readiness
)

Cognitive Readiness = clamp0to100(
    0.35 * hydrationScore
  + 0.40 * sleepScore
  + 0.25 * hrvNormalized                        // hrvNormalized mapped 0–100 vs personal baseline
)

Band thresholds (reuse the existing convention exactly):
PEAK 90–100 · BALANCED 75–89 · RECOVERING 60–74 · DEPLETED 0–59

Data-sufficiency rule:


Muscle Readiness requires hydrationScore AND recoveryCapacity present. workoutLoad may be absent (treat fatigue as 0).
Cognitive Readiness requires hydrationScore AND sleepScore present. hrv may be absent (drop the HRV term and renormalize the remaining weights to sum to 1.0).
If a required input is missing, return hasEnoughData: false and a "Keep logging to unlock your read" state — NEVER fabricate or default a score.


Round every displayed number with Math.round at the boundary, not mid-calculation.


REQUIRED UNIT TESTS (utils/metabolicScore.test.ts — write these before any UI consumes the math)


Known-value cases — assert the exact rounded outputs hard-coded below.
Clamp behavior — inputs that would mathematically exceed 100 or fall below 0 return exactly 100 / 0.
Band mapping — assert each band boundary resolves to the correct label; test the off-by-one edges explicitly.
Missing-input handling:

Muscle Readiness with recoveryCapacity undefined → hasEnoughData false, no number returned.
Cognitive Readiness with hrv undefined → returns a score using renormalized weights (NOT a penalized score).
Any required input undefined → never returns NaN.



Determinism — same inputs always yield the same output (no Date.now()/random leakage into the math).
ISOLATION GUARANTEE (the critical one) — a test that calls every exported metabolic function and then asserts the hydration store and recovery store are byte-for-byte unchanged before vs. after. This is the proof that the new layer cannot mutate the engine. If you cannot write this test cleanly, the dependency direction is wrong — fix the architecture, don't weaken the test.


HAND-CALCULATED KNOWN-VALUE CASES (assert these exact integers)

Muscle Readiness = round( 0.40*hydration + 0.40*recovery + 0.20*(100 - fatigue) )

CASE M1 — PEAK
  inputs:  hydrationScore = 95, recoveryCapacity = 92, workoutFatigue = 10
  math:    0.40*95 + 0.40*92 + 0.20*(100-10) = 38.0 + 36.8 + 18.0 = 92.8
  expected: score = 93, band = "PEAK"

CASE M2 — RECOVERING
  inputs:  hydrationScore = 68, recoveryCapacity = 70, workoutFatigue = 55
  math:    0.40*68 + 0.40*70 + 0.20*(100-55) = 27.2 + 28.0 + 9.0 = 64.2
  expected: score = 64, band = "RECOVERING"

CASE M3 — DEPLETED
  inputs:  hydrationScore = 45, recoveryCapacity = 40, workoutFatigue = 80
  math:    0.40*45 + 0.40*40 + 0.20*(100-80) = 18.0 + 16.0 + 4.0 = 38.0
  expected: score = 38, band = "DEPLETED"

CASE M4 — fatigue absent (treat as 0)
  inputs:  hydrationScore = 80, recoveryCapacity = 80, workoutFatigue = undefined
  math:    0.40*80 + 0.40*80 + 0.20*(100-0) = 32.0 + 32.0 + 20.0 = 84.0
  expected: score = 84, band = "BALANCED"

CASE M5 — recoveryCapacity absent (required input missing)
  inputs:  hydrationScore = 80, recoveryCapacity = undefined
  expected: hasEnoughData = false, no numeric score returned

Cognitive Readiness = round( 0.35*hydration + 0.40*sleep + 0.25*hrvNormalized )

CASE C1 — PEAK
  inputs:  hydrationScore = 92, sleepScore = 95, hrvNormalized = 88
  math:    0.35*92 + 0.40*95 + 0.25*88 = 32.2 + 38.0 + 22.0 = 92.2
  expected: score = 92, band = "PEAK"

CASE C2 — RECOVERING
  inputs:  hydrationScore = 70, sleepScore = 65, hrvNormalized = 60
  math:    0.35*70 + 0.40*65 + 0.25*60 = 24.5 + 26.0 + 15.0 = 65.5
  expected: score = 66, band = "RECOVERING"

CASE C3 — DEPLETED
  inputs:  hydrationScore = 50, sleepScore = 45, hrvNormalized = 40
  math:    0.35*50 + 0.40*45 + 0.25*40 = 17.5 + 18.0 + 10.0 = 45.5
  expected: score = 46, band = "DEPLETED"

CASE C4 — HRV absent (renormalize remaining weights to sum to 1.0)
  inputs:  hydrationScore = 80, sleepScore = 90, hrv = undefined
  renormalized weights: hydration 0.35/0.75 = 0.4667, sleep 0.40/0.75 = 0.5333
  math:    0.4667*80 + 0.5333*90 = 37.333 + 48.0 = 85.333
  expected: score = 85, band = "BALANCED"
  NOTE: do NOT treat missing HRV as 0 — that would give 64 and unfairly penalize users
  without an HRV source. Rescale the remaining weights instead.

CASE C5 — sleepScore absent (required input missing)
  inputs:  hydrationScore = 80, sleepScore = undefined
  expected: hasEnoughData = false, no numeric score returned

Band-boundary cases (test the off-by-one edges explicitly):

  score 59 → "DEPLETED"     score 60 → "RECOVERING"
  score 74 → "RECOVERING"   score 75 → "BALANCED"
  score 89 → "BALANCED"     score 90 → "PEAK"
  score 0  → "DEPLETED"     score 100 → "PEAK"

Clamp cases:

  Muscle:    hydration=100, recovery=100, fatigue=0  → raw 100.0 → score 100 (not 101+)
  Cognitive: hydration=100, sleep=100, hrv=100       → raw 100.0 → score 100
  Any construction yielding a negative raw value      → score 0 (never negative)


EXECUTION ORDER


First, read and summarize back to me: the existing flag shape, the entitlement inheritance chain, and the hydrationScore util style. Confirm your understanding BEFORE writing code.
Build D → C → B → A → E in dependency order, with the pure math (C) unit-tested before anything consumes it.
Write tests proving the metabolic layer cannot mutate hydration/recovery state.
Show me a diff summary and confirm the flag-off path returns the app to baseline.


Work like the best engineer on the team: read before you write, match existing conventions over inventing new ones, keep the dependency graph one-directional, and leave the core engine untouched. Ask me before making any architectural decision that isn't specified here.


BEFORE YOU START — context the agent needs

If the AForce OS repo is not the active project, the following existing files must be visible so conventions can be matched (not guessed): featureFlags/flags.ts, useEntitlement.ts, data/subscriptionPlans.ts, utils/hydrationScore.ts, services/recoveryEngine.ts, services/healthConnection.ts, theme/colors.ts, theme/typography.ts, app/(tabs)/index.tsx.