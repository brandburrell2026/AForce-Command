Approved — proceed with the migration. Decisions on your three questions:

1. LIME #B6FF00 — split by role, lime survives NOWHERE:
   - Where lime was the BRAND hero (CTAs, tabBar.active, eyebrows, thin lines, 
     accent.primary/glow/dim/subtle, non-status accents) → signal red #C1281B.
   - Where lime was a STATUS band (states.PEAK, statusColor STABLE/Optimal, the 
     orb at peak) → Soursop Edge green #1FA35A. Do NOT make PEAK red — red is 
     the danger/DEPLETED color and PEAK must not collide with it.
   - Retune the score-status ladder so it reads green→amber→red:
       Optimal/PEAK  → #1FA35A (Soursop green)
       Stable        → #1FA35A (or a slightly lighter green tint if you need 
                        two green steps — your call, keep it green)
       Declining     → keep amber (#FFDE00 / existing)
       Risk          → keep orange (existing)
       Critical/DEPLETED → keep red (#FF0026 / existing)
   - Verify the orb still reads correctly at every band after the swap.

2. TEAL #0093E7 (accent.secondary + info) → Berry Blast blue #1E5BFF 
   (our locked flavor blue). No competitor blue as a generic info color.

3. TYPOGRAPHY — COLOR PASS ONLY this session. 
   - Install @expo-google-fonts Archivo Black + IBM Plex Mono and load them in 
     app/_layout.tsx useFonts.
   - Wire them into the TYPE TOKENS by role only (display-hero/display-score/
     display-title/h1 → Archivo Black; eyebrow/metric-label/mono labels → 
     IBM Plex Mono; body stays Inter).
   - Do NOT touch the hundreds of hardcoded Inter_* call sites yet — that's a 
     separate follow-up pass. Keep this PR color-focused and reviewable.

ALSO PROCEED WITH (as you described):
- #FF3B30 → #C1281B everywhere (use signal red, not Apple system red).
- Solid backgrounds #000000 → #0D0D0D ONLY. Leave rgba(0,0,0,α) scrims, 
  shadowColor:'#000000', text.inverse, and the 2 test files as pure black.
- Rename "WHOOP-Cinematic Edition" → "AForce Brand System" in 
  design/aforce-tokens.json (version string) and aforce-design-tokens.md; 
  strip WHOOP-derived language from the design philosophy section.
- Investor Demo: confirmed no gold in code — it's BAND_ACCENT-driven and will 
  inherit the retuned tokens automatically. Just verify the wordmark/score reads 
  in signal red or the correct band color after retune. No literal edits needed.

GUARDRAILS:
- Re-skin only. No layout, nav, tab, or engine-logic changes.
- Keep all 883 tests green. Where a color snapshot/assertion breaks, update the 
  expected value to the new brand token — do NOT weaken the test. The 
  statusColor.test.ts lime assertion should now expect green.
- After: run typecheck + full suite, export the updated aforce-tokens.json 
  (Tokens Studio format), and give me a one-paragraph change summary + any value 
  you couldn't safely map.