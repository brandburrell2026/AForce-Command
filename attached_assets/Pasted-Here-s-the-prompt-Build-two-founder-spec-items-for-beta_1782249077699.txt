Here's the prompt:

---

**Build two founder-spec items for beta: #3 Performance Memory (full) and #10 Performance Identity (type + capture + signal scaffolding, classifier inert). Follow STEP 0 rules: audit existing code first, reuse don't rebuild, no breaking changes, no speculative scope beyond what's specified here. The one critical line: build everything for Identity EXCEPT the archetype-assignment logic — `archetype` and `confidence` stay unpopulated during beta, populated later by a post-beta classifier derived from real beta data. Do not hand-tune a classifier against assumptions now.**

**STEP 0 — audit before building.** Read the existing stores the prior audit identified: `state.history`, `intelligence/commandEvents.ts`, `executionMemory.ts`, `performanceMemory.ts`, the Performance Age snapshots, and the intake/fluidType path for caffeine. Report how each works. The new layer must READ THROUGH these — they remain source-of-truth and must keep working unchanged. `commandEvents` feeds Command Confidence; do not touch its behavior.

**PHASE 1 — Performance Memory (build fully):**
1. One unified `PerformanceMemory` model that aggregates and normalizes the existing stores through a single read-through interface. Existing stores stay source-of-truth; this does not replace or duplicate them. Fields: hydrationPatterns, caffeinePatterns, travelPatterns, userPriorities, commandHistory, completionHistory, executionStreaks, checkInHistory, recoveryPatterns, performanceAgeHistory, preferredCommandTypes, lastUpdated.
2. Add longitudinal capture for the three patterns currently missing: **travel**, **caffeine** (today only logged as an intake fluidType with no trend memory — add trend capture), **userPriorities**. Capture starts accumulating on build — this is time-sensitive; un-captured beta data is unrecoverable.
3. **Retention is bounded, not unlimited.** Do not "remove all history limits." Pick a concrete retention window appropriate for trend analysis — propose a specific number (e.g. 180 days or 12 months) and state it, factoring storage/performance cost. Flag it for my confirmation rather than defaulting to unbounded.
4. Memory governance: user can view stored memory, user can delete it, memory persists across app updates, removed only on explicit user request. Make the delete path real, not a stub.
5. Observed behavior only — no inference of personality, psychological traits, or medical conditions. Store measurable activity and user-provided inputs only.
6. Score Protection holds: Performance Memory is read/observational, never awards or mutates score. Confirm in implementation.

**PHASE 2 — Performance Identity (foundation + signals, NO classifier):**
1. Define `PerformanceIdentity` type: `{ archetype, confidence, supportingSignals, lastUpdated }`, in the appropriate store/schema matching existing patterns.
2. **Build the signal-capture scaffolding fully** — `supportingSignals` accumulates real observed signal during beta (consistency, completion rate, recovery behavior, streak behavior, adherence). This part is live.
3. **The classifier is INERT for beta.** `archetype` and `confidence` are NOT populated by assignment logic. Leave them null/unset, populated later by a post-beta classifier (separate task). Do NOT write code that assigns Operator/Warrior/Optimizer/Builder/Recoverer or computes a confidence %. The five archetype names may exist as a type enum/constant only — no logic that maps a user to one.
4. **Admin visibility shows raw signals, not verdicts.** The internal/admin view displays the accumulated `supportingSignals` for a user (e.g. "92% completion, strong morning adherence, 14-day streak") — it does NOT display "Operator, 78%," because nothing is classifying yet. Internal-only; never exposed to end users; never used for personalization, command selection, scoring, or recommendations.
5. Identity may observe from Performance Memory; it may NOT change commands, scores, recommendations, Performance Age, or Command Confidence.

**If building Phase 2's scaffolding tempts you toward writing assignment logic, STOP and flag it — do not write it.** Building the classifier is the deferred, data-dependent task; building it now against assumptions is the specific thing this prompt exists to prevent.

**FINAL BETA RULES — do NOT:** build new dashboards unless required, build new scoring systems, build recommendation engines, modify Command Confidence / Performance Age / Evidence Engine, introduce ML or LLM decision-making. **DO:** reuse existing architecture, keep changes minimal, explainable, auditable.

**Build #3 first (real gap + capture clock), stop and report, then #10. Deliverables: updated architecture diagram, the two models, migration plan for existing sources, integration points, test coverage, and a verification report proving no regression to existing beta functionality AND confirming zero archetype-assignment logic was written.**

---

The three lines doing the real work: "read through, source-of-truth unchanged" (protects Command Confidence), "retention bounded, propose a number" (kills the scope creep in the original doc), and the inert-classifier block repeated three ways (so it can't drift into building the deferred half).

Your tell when it returns: if the verification report can't say "zero archetype-assignment logic written" — or if the admin view shows "Operator, 78%" anywhere — it overstepped, and you send it back. The admin view showing raw signals instead of verdicts is the cleanest single check.

Run that, and when it's back we go to Kristel/Peter and Amato — still need the 200-vs-500 source and what's stale in the memo.