Update the AForce OS platform architecture so it is designed to scale to 50 million+ concurrent users globally with minimal latency, high availability, and zero single points of failure.

IMPORTANT:
This is not a small app architecture.
This is not startup MVP infrastructure.
This is global-scale, mission-critical system design.

The system must be architected as if AForce OS can support:
- 50,000,000+ active users
- millions of concurrent API requests
- real-time updates
- near-zero downtime
- graceful degradation under extreme load
- no crashes during spikes
- fast recovery from failures

The final output should include:
- scalable system architecture
- infra recommendations
- backend service structure
- database strategy
- caching strategy
- queue/event architecture
- real-time communication design
- observability + failover strategy
- security + rate limiting
- load testing strategy
- code structure recommendations
- deployment recommendations

==================================================
CORE SCALING GOAL
==================================================

AForce OS must be able to handle:
- 50M+ users
- global traffic
- high burst usage during product launches, major events, sports seasons, and viral spikes
- real-time state updates, scoring, competition, AI commands, Heat Guard, voice, scanning, and team environments

The system must feel:
- instant
- resilient
- distributed
- enterprise-grade
- globally scalable

==================================================
ARCHITECTURE PRINCIPLES
==================================================

Design the system using these principles:

1. Horizontal scalability first
2. Stateless app services wherever possible
3. Event-driven processing
4. Cache-first read patterns
5. Queue-based async workloads
6. Region-aware infrastructure
7. Graceful degradation over total failure
8. Multi-layer failover
9. No single point of failure
10. Real-time systems separated from heavy compute systems

==================================================
HIGH LEVEL ARCHITECTURE
==================================================

Design AForce OS as a distributed platform with these major layers:

1. Edge / CDN / API Gateway Layer
2. Authentication Layer
3. Core Application Services Layer
4. Real-Time State Layer
5. AI Decisioning Layer
6. Event Ingestion Layer
7. Queue / Worker Layer
8. Analytics / Warehouse Layer
9. Competition / Leaderboard Layer
10. Hardware / BLE / Device Sync Layer
11. Notification Layer
12. Observability / Alerting Layer

==================================================
INFRASTRUCTURE REQUIREMENTS
==================================================

Recommend and structure for:
- multi-region deployment
- autoscaling containers
- Kubernetes or equivalent orchestration
- global load balancing
- edge caching
- CDN for assets and static content
- region failover
- rolling deploys
- blue/green deploys or canary deploys

Recommended infra stack should include placeholders for:
- Cloudflare or equivalent edge layer
- AWS / GCP / Azure compatible architecture
- Kubernetes
- Redis cluster
- PostgreSQL sharding / partitioning strategy
- Kafka or equivalent event streaming
- worker queues
- object storage
- observability stack

==================================================
SERVICE DESIGN
==================================================

Split the backend into scalable services.

Create recommended microservice or modular service boundaries for:

1. Auth Service
2. User Profile Service
3. Current State Service
4. Scoring / Protocol Engine
5. Pulse Service
6. AI Command Service
7. Intake / Logging Service
8. Hydration Scan Service
9. Competition Service
10. Subscription / Billing Service
11. Team / Clutch Service
12. Guardian / Risk Engine Service
13. Notification Service
14. Hardware Sync Service
15. Analytics Service

Each service should define:
- purpose
- scaling pattern
- sync vs async responsibilities
- data ownership
- cache needs
- failure modes

==================================================
DATABASE STRATEGY
==================================================

Design a database strategy that can scale to 50M+ users.

Requirements:
- PostgreSQL or equivalent for core relational data
- partitioning and sharding plan
- read replicas
- write optimization
- hot path isolation
- immutable event log strategy
- archival strategy
- region-aware reads

Separate data by use case:

1. Transactional relational data
- users
- profiles
- subscriptions
- teams
- billing

2. Real-time fast state
- current_user_state
- pulse_config
- next_command
- live competition rank slices

3. Immutable event data
- raw_events
- intake logs
- symptom logs
- scan events
- command events
- protocol outcomes

4. Analytics / warehouse
- long-term history
- cohort metrics
- leaderboard rollups
- predictive model data

Recommend:
- what stays in Postgres
- what moves to Redis
- what moves to event streams
- what moves to warehouse / object storage

==================================================
CACHING STRATEGY
==================================================

Design a Redis or equivalent cache strategy for:
- current user state
- pulse configuration
- home screen payload
- leaderboard slices
- subscription flags
- team state
- Guardian hot alerts
- Heat Guard hot alerts

Rules:
- cache first on read-heavy flows
- define TTL strategies
- define invalidation patterns
- define fallback if cache misses occur
- avoid stale critical safety data

==================================================
EVENT DRIVEN ARCHITECTURE
==================================================

AForce OS should use an event-driven architecture.

All major actions should emit events, including:
- intake logged
- symptom updated
- urine signal updated
- energy state updated
- protocol completed
- AI command generated
- heat risk changed
- competition rank changed
- hardware signal received
- subscription changed

Use an event bus or stream system such as Kafka or equivalent.

Design:
- event schemas
- topic separation
- consumer groups
- replayability
- dead letter queues
- exactly-once or idempotent handling strategy

==================================================
REAL-TIME SYSTEMS
==================================================

Design real-time communication for:
- score updates
- pulse updates
- competition leaderboards
- live team command grid
- Guardian alerts
- Heat Guard alerts
- hardware sync state

Recommend using:
- WebSockets
- SSE
- pub/sub
- fanout layer

Define:
- what is real-time
- what can be polled
- what should be throttled
- how to handle massive fanout under load

==================================================
AI LAYER SCALING
==================================================

Design the AI Decisioning Layer for global scale.

Requirements:
- never block the full app on AI delay
- fallback behavior if AI provider is slow
- deterministic response templates for degraded mode
- caching for repeated command patterns
- asynchronous generation where possible
- rate limiting per user/session
- context precomputation

Define architecture for:
- AI request router
- provider abstraction
- primary / fallback providers
- structured output validator
- deterministic command fallback templates
- queue-based non-urgent AI tasks

The app must continue functioning even if external AI providers partially fail.

==================================================
RATE LIMITING + PROTECTION
==================================================

Add abuse protection and resilience planning for:
- rate limiting
- DDoS mitigation
- abusive scan traffic
- abusive voice command loops
- bot competition spam
- leaderboard gaming
- duplicate event submission
- replay attack prevention

Define:
- IP rate limits
- user-level rate limits
- endpoint-specific controls
- idempotency keys
- auth token expiration and refresh

==================================================
OBSERVABILITY + INCIDENT RESPONSE
==================================================

Design full observability.

Include:
- logs
- metrics
- traces
- SLOs
- SLIs
- dashboards
- alerts
- synthetic monitoring
- health checks
- runbooks

Track:
- p95 latency
- p99 latency
- error rates
- queue lag
- cache hit ratio
- DB replication lag
- websocket connection counts
- AI provider failure rates
- event processing lag
- region health

Add incident design for:
- partial region failure
- DB saturation
- Redis outage
- AI outage
- queue backlog
- competition spike
- broadcast event spikes

==================================================
FAILOVER + RESILIENCE
==================================================

Design for:
- multi-region active / active or active / passive
- DB failover
- cache failover
- queue failover
- circuit breakers
- retries with backoff
- degraded mode UI
- kill switches / feature flags
- traffic shedding during overload

Examples of degraded mode:
- static command templates if AI fails
- cached leaderboard if live ranking is degraded
- pulse still functional if analytics is delayed
- Home screen still works if Competition service is degraded

==================================================
LOAD TESTING STRATEGY
==================================================

Create a load testing and scale validation plan.

Simulate:
- 100K concurrent users
- 1M concurrent users
- 10M concurrent users
- 50M+ burst architecture assumptions

Test:
- home screen reads
- intake logging
- protocol updates
- live leaderboard updates
- scan events
- voice events
- AI command generation
- Clutch team dashboards
- Guardian risk alerts

Define:
- tools to use
- what to benchmark
- acceptable latency targets
- bottleneck detection process

==================================================
PERFORMANCE TARGETS
==================================================

Define target performance standards:

- Home payload p95 under 300ms
- intake log p95 under 200ms
- pulse update under 150ms
- AI command under 1 second in normal mode
- leaderboard fetch under 300ms cached
- competition update fanout near real-time
- graceful fallback under heavy spike
- 99.99% uptime target or clearly specified high-availability target

==================================================
CODEBASE / REPLIT REQUIREMENT
==================================================

Inside Replit, do NOT actually build 50M-scale infra.
Instead, generate:

1. A production-ready architecture blueprint in code comments / markdown
2. A modular backend skeleton showing service separation
3. Example config files
4. Example service interfaces
5. Event schemas
6. Queue job definitions
7. Caching wrappers
8. Rate limiter middleware
9. Health check endpoints
10. Observability hooks
11. Feature flag kill switch examples
12. Load test spec files
13. README explaining scaling architecture

Build the codebase so engineers can extend it into real infra later.

==================================================
FILES TO CREATE
==================================================

Create files such as:
- /docs/scaling-architecture.md
- /docs/load-testing-plan.md
- /docs/failover-strategy.md
- /src/services/*
- /src/events/*
- /src/queues/*
- /src/cache/*
- /src/middleware/rateLimiter.ts
- /src/middleware/idempotency.ts
- /src/observability/*
- /src/health/*
- /src/config/*
- /src/types/*
- /README.md

==================================================
DELIVERABLE
==================================================

Generate a full, structured, engineering-grade architecture package for AForce OS showing exactly how the software should be designed to support 50M+ users concurrently with minimal delays, strong resilience, and graceful degradation.

The result should feel like:
- a global-scale system blueprint
- a serious enterprise architecture
- a platform that can survive explosive growth

FINAL GOAL:
Any engineer reading the output should understand how to build AForce OS into a globally scalable platform that can handle massive concurrency without crashing.