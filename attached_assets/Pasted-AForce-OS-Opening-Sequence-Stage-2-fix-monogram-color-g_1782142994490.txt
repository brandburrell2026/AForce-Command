AForce OS — Opening Sequence Stage 2: fix monogram color, glow, and the 
mirrored N

CONTEXT
File: components/opening/OpeningSequence.tsx, Stage 2. The current build (see 
screenshot) has three problems to fix. This is a re-skin + glyph fix of Stage 2 
ONLY — do not change stage count, timing, skip-to-tap, reduce-motion, or any 
other stage. Display-only, score-protection unchanged.

FIX 1 — MONOGRAM COLOR (remove green as the hero):
- The N–N monogram and the "AFORCE" wordmark currently render green/washed 
  green. Change the monogram and wordmark to BONE #F5F0E8 (silver-white hero) 
  on the cinematic black #0D0D0D canvas.
- Soursop green #1FA35A is NOT used as a hero or fill on this screen. Remove it.
- Signal red #C1281B is the ONLY accent — used solely on the thin hairlines 
  above and below the caption (keep those as-is, they read well).

FIX 2 — THE GLOW (green halo → bone/silver):
- Replace the large green halo circle behind the mark with a soft BONE/SILVER 
  radial glow: low opacity (~0.06–0.10), large blur, no hard edge, no visible 
  circle outline. It should read as ambient light off metal (nod to the can's 
  silver), not a solid disc. Never a hard box shadow.
- The glow sits behind the monogram only and must not muddy the wordmark or 
  caption legibility.

FIX 3 — MIRROR THE N (this is the canonical brand mark — currently missing):
- The monogram must read N–И : a normal "N", an en-dash, then a MIRRORED "N" 
  (horizontally flipped). The screenshot shows two normal N's — that's wrong.
- Flip the SECOND N only via transform scaleX(-1) on that glyph. It must be a 
  real "N" glyph flipped (Archivo Black weight), NOT the Cyrillic И or any 
  lookalike character.
- The caption must read "NON — ИEGOTIABLE": the N that begins NEGOTIABLE is 
  also MIRRORED (scaleX(-1) on that glyph only). "NON" stays normal. Match the 
  can exactly.
- Build the caption as individual glyph spans so the single mirrored N flips in 
  isolation without breaking kerning of the surrounding letters. Verify spacing 
  reads cleanly after the flip — no overlap, no gap.

TYPE:
- Monogram + wordmark: Archivo Black, bone #F5F0E8.
- Eyebrow "PERFORMANCE IS" and caption: IBM Plex Mono, bone #F5F0E8, uppercase, 
  tracked (~3px eyebrow). The mirrored N in the caption should still be the 
  display/mono face consistent with how it renders now, just flipped.

GUARDRAILS:
- Use brand tokens, not hardcoded hex.
- Reanimated 3, existing crossfade/scale motion preserved. Reduce-motion still 
  skips animation and fades final state in. Tap-anywhere-to-skip works every frame.
- Keep tests green; update any OpeningSequence snapshot to the new expected 
  output rather than weakening it.

DELIVERABLE: updated Stage 2, a note on how the mirrored-N transform is done 
(monogram second-N + caption NEGOTIABLE-N), and confirmation skip + reduce-motion 
still work. Capture a fresh simulator screenshot so I can verify the mirror and 
the bone/silver treatment landed.