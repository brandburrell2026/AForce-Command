AForce OS — Brand System Re-Migration (WHOOP-Cinematic → AForce Locked Brand)

CONTEXT
The current design tokens are "WHOOP-Cinematic Edition" (pure black #000000, 
WHOOP lime #B6FF00 hero accent). This is WRONG — it reverted to a competitor's 
palette. Migrate to the locked AForce brand system below. This is a TOKEN + 
HARDCODED-VALUE swap, NOT a redesign. Do not move navigation, add tabs, change 
layouts, or touch engine logic. Re-skin only.

ANALYZE FIRST (do not edit yet — report back):
1. Read design/aforce-tokens.json and design/aforce-design-tokens.md
2. Grep the entire artifacts/aforce-os codebase for hardcoded values that bypass 
   tokens: #B6FF00, #000000, #FF3B30, #0093E7, #E8613A, and any "gold" usages 
   (esp. components/opening/ and the InvestorDemo overlay).
3. List every file with hardcoded brand hex so we know the blast radius.
   STOP and show me the list before applying changes.

TARGET BRAND SYSTEM (locked — source of truth):
Core:
  bg.primary / canvas:      #0D0D0D   (cinematic black — NOT #000000)
  text on dark / bone:      #F5F0E8   (bone)
  accent.brand / hero:      #C1281B   (signal red — replaces lime #B6FF00 everywhere)
Flavor accents (keep as flavor-scoped only, never global hero):
  Berry Blast:              #1E5BFF
  Watermelon Surge:         #E5384B
  Soursop Edge:             #1FA35A
Typography:
  display:    Archivo Black
  technical:  IBM Plex Mono
  body:       Inter (keep)

MAPPING RULES:
- Hero accent #B6FF00 (lime) → #C1281B (signal red). This includes accent.primary, 
  accent.glow, accent.dim, accent.subtle, orb glow, CTA glow, tabBar.active, 
  active/selected states.
- Canvas #000000 → #0D0D0D across bg.primary and any hardcoded pure-black backgrounds.
- The accent override #FF3B30 (Apple system red) → #C1281B (signal red). We use 
  signal red, not the iOS default.
- Investor Demo (Phase 10) Act 1 "wordmark fades in gold" → fades in signal red 
  #C1281B. Remove all gold.
- Performance-state bands (Peak/Balanced/Recovering/Depleted) and the 5 score-status 
  bands: KEEP these functional status colors as-is — DEPLETED red, recovering amber, 
  etc. must still read. Only the BRAND hero accent changes, not semantic status colors.

DEPRECATION CLEANUP:
- The doc says teal is deprecated as of 2026-06-01 but still lists 
  accent.secondary #0093E7 ("WHOOP teal"). Remove or remap it — it must not appear 
  as a brand accent. Same for coral #E8613A if it's only a leftover pulse-ring value.

TYPOGRAPHY:
- Add Archivo Black (display scale: display-hero, display-score, display-title, h1) 
  and IBM Plex Mono (eyebrows, metric-labels, technical/mono labels) to the type 
  system. Keep Inter for body. Use @expo-google-fonts packages, load in the root 
  font loader, and wire the existing type tokens to the new families.

RENAME:
- Update design/aforce-design-tokens.md and the JSON version string: 
  "WHOOP-Cinematic Edition" → "AForce Brand System". Remove WHOOP-derived language 
  in the design philosophy section. The source of truth should not read as a WHOOP 
  derivative.

CONSTRAINTS (hard):
- One change at a time where it makes sense; this is a refinement, not a rebuild.
- Do not alter navigation, tabs, layouts, score logic, or feature flags.
- Keep all 883 tests green. If a snapshot/color test breaks, update the expected 
  value to the new brand token — do not weaken the test.
- After changes: run typecheck + full test suite, and export the updated 
  aforce-tokens.json (Tokens Studio format) so I can re-import to Figma.

DELIVERABLE:
1. The blast-radius report (before editing).
2. The migrated token files + font wiring + Investor Demo color fix.
3. Confirmation that typecheck + tests pass.
4. A one-paragraph summary of what changed and any hardcoded values you couldn't 
   safely auto-map (flag for my review).