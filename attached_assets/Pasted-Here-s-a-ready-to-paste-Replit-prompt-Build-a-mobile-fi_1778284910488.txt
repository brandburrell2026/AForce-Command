Here's a ready-to-paste Replit prompt:

---

**Build a mobile fitness app home screen called "AForce OS" using React + Tailwind CSS (Vite setup).**

The app should look like a premium dark-mode performance tracking app — think cinematic, brutal, editorial. Black background (#080808), crimson red accent (#E01818), white text hierarchy.

**Typography:** Import `Bebas Neue` (display/numbers) and `DM Sans` (body/labels) from Google Fonts.

**Layout (single scrollable mobile screen, max-width 380px centered):**

1. **Status bar** — time left, signal + battery icons right
2. **Live pill badge** — pulsing red dot + "LIVE MONITORING" label in a subtle red-tinted pill
3. **Hero wordmark** — "AFORCE" then "OS" in red, Bebas Neue, ~92px, tight leading
4. **Subtitle** — "THE PERFORMANCE OPERATING SYSTEM" spaced caps, muted
5. **Readiness Ring** — SVG circle ring animates on load (stroke-dashoffset), shows 83 score in center with "READY" label. Right side shows 4 stat rows: Strain / Recovery / Sleep / HRV with color-coded values (green = good, amber = warning)
6. **HRV Sparkline** — SVG area chart with green line + gradient fill, 7-day trend, live dot at endpoint, current value top-right
7. **Metrics 2x2 grid** — cards for Resting HR, Active Burn, SpO₂, Recovery Time. Each has an emoji icon, large Bebas Neue number, muted uppercase label. Cards have a red underline accent on hover.
8. **Tagline block** — "Performance is non-negotiable." in light weight with bold emphasis. Below: 3 pill tags — Closed-loop · Real-time · Deterministic
9. **CTA buttons** — Full-width red "BEGIN PROTOCOL" button with arrow badge, ghost secondary "View Full Report" below
10. **Bottom tab nav** — 5 tabs: Home (active, red), Metrics, Strain, Sleep, Profile

**Visual effects:**
- Subtle grain texture overlay (SVG feTurbulence filter, low opacity)
- Radial red glow orb behind the hero text, slow pulse animation
- All sections animate in with staggered slide-up on mount (use `animation-delay` or framer-motion)
- Ring stroke draws itself on load over 1.5s
- Hover states on metric cards reveal the red accent bar

**Color tokens:**
- Background: `#080808`
- Red: `#E01818`
- Green (good): `#4ade80`
- Amber (warn): `#f59e0b`
- Text primary: `#ffffff`
- Text muted: `rgba(255,255,255,0.35)`
- Card bg: `rgba(255,255,255,0.04)`
- Card border: `rgba(255,255,255,0.08)`

Keep everything in one component file. No routing needed. Make it pixel-perfect and production-quality.