Here's a prompt you can paste directly into Replit Agent:

---

**AForce OS — Code Quality Fixes**

You are working on an Expo Router app (`artifacts/aforce-os/app/`). Fix the following issues in priority order. Do not change any UI, navigation structure, feature flags, or business logic beyond what is described.

---

**1. Extract shared Clerk error helper**
Create `utils/clerkErrors.ts`:
```ts
export function extractClerkError(err: unknown): string | null {
  if (!err || typeof err !== 'object') return null;
  const e = err as { errors?: Array<{ message?: string; longMessage?: string }>; message?: string };
  if (Array.isArray(e.errors) && e.errors.length > 0) {
    return e.errors[0]?.longMessage ?? e.errors[0]?.message ?? null;
  }
  return e.message ?? null;
}
```
Then remove the duplicate `extractClerkError` function from both `app/(auth)/sign-in.tsx` and `app/(auth)/sign-up.tsx` and import it from `@/utils/clerkErrors`.

---

**2. Fix SSO `setActive` navigation in both auth screens**
In `app/(auth)/sign-in.tsx` and `app/(auth)/sign-up.tsx`, replace the `navigate` callback shape inside `ssoSetActive`:
```ts
// REMOVE this pattern:
await ssoSetActive({
  session: createdSessionId,
  navigate: ({ session }) => {
    if (session?.currentTask) return;
    router.replace('/(tabs)');
  },
});

// REPLACE with:
await ssoSetActive({ session: createdSessionId });
router.replace('/(tabs)');
```

---

**3. Add production guard for DEMO_MODE**
In `services/demoMode.ts` (or wherever `DEMO_MODE` is defined), add immediately after the constant:
```ts
if (typeof __DEV__ !== 'undefined' && !__DEV__ && DEMO_MODE) {
  throw new Error('DEMO_MODE must not be enabled in production builds. Set DEMO_MODE to false.');
}
```

---

**4. Separate Ring and Phantom feature flags**
In `types/index.ts` (or wherever `FeatureFlags` is defined), add:
```ts
ring_enabled: boolean;
```
In `featureFlags/flags.ts`, add `ring_enabled: false` to `DEFAULT_FLAGS` and `ring_enabled: true` to `DEMO_ALL_ON_FLAGS`.

Then update `app/ring.tsx` and `app/ring/session.tsx` to gate on `flags.ring_enabled` instead of `flags.phantom_wearable_enabled`.

---

**5. Fix `handleConfirm` race condition in Check screen**
In `app/(tabs)/check.tsx`, wrap `handleConfirm` in try/catch so `confirmStatus` is not called if the updates fail:
```ts
const handleConfirm = async () => {
  Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Heavy);
  try {
    await Promise.all([
      updateSymptoms(symptoms),
      updateUrineSignal(urine),
      updateEnergyState(energy),
    ]);
    await confirmStatus();
  } catch (err) {
    console.error('Confirm status failed:', err);
    // optionally surface a toast here
  }
};
```

---

**6. Fix incomplete `useMemo` deps in Protocol screen**
In `app/(tabs)/protocol.tsx`, update the `useMemo` dependency array to depend on the full objects instead of cherry-picked fields, since `deriveProtocol` receives both full objects:
```ts
const protocol = useMemo(
  () => deriveProtocol(userState, engineOutput),
  [userState, engineOutput]
);
```

---

**7. Fix haptics unhandled rejections on web**
In `app/(tabs)/check.tsx` and `app/(tabs)/profile.tsx`, replace all direct calls to `Haptics.selectionAsync()` and `Haptics.impactAsync()` with the safe lazy-import pattern:
```ts
import('expo-haptics').then(m => m.selectionAsync().catch(() => {}));
// or for impact:
import('expo-haptics').then(m => m.impactAsync(m.ImpactFeedbackStyle.Heavy).catch(() => {}));
```

---

**8. Wire real Clerk user data into Profile display**
In `app/(tabs)/profile.tsx`, `useUser()` is already imported. Replace the mock name and avatar initial with real data where available:
```ts
const { user } = useUser();
const displayName = user?.fullName ?? user?.firstName ?? mockUserProfile.name;
const avatarInitial = displayName.charAt(0).toUpperCase();
```
Use `displayName` wherever `mockUserProfile.name` is currently displayed, and `avatarInitial` for the avatar. Leave all other `mockUserProfile` fields (weight, target, tier, etc.) unchanged — those are not yet wired to a real API.

---

**9. Extract web layout padding constants**
Create `constants/layout.ts`:
```ts
export const WEB_TOP_PADDING = 67;
export const WEB_BOTTOM_INSET = 34;
export const TAB_BAR_HEIGHT = 84;
export const WEB_BOTTOM_PADDING = WEB_BOTTOM_INSET + TAB_BAR_HEIGHT;
```
Replace all hardcoded `67`, `34 + 84`, and `34` web padding values across every screen with imports from this file.

---

**10. Fix tab count comment in layout**
In `app/(tabs)/_layout.tsx`, update the file header comment from:
```
* 5 tabs: Home · Check · Protocol · Store · Profile
```
to:
```
* 6 tabs: Home · Check · Protocol · Journal · Store · Profile
```

---

**Do not:**
- Change any screen UI or styling
- Modify any scoring engine logic
- Alter navigation structure or route names
- Touch any files not mentioned above

**After making all changes**, check that TypeScript compiles without errors (`npx tsc --noEmit`) and that no import paths are broken.

---

That's everything. Paste it as-is and Replit Agent should handle all 10 fixes in a single pass without touching anything it shouldn't.