Here's the prompt:

---

**AForce OS — Hydration Journal Fixes**

You are working on an Expo Router app. The Hydration Journal screen is at `app/(tabs)/journal.tsx` which mounts `screens/JournalScreen`. Fix the following issues in priority order. Do not change any other screens, navigation, or business logic.

---

**1. Fix the zero floor on the chart**

In the chart data preparation logic, any score value of `0` or `null` or `undefined` should be treated as a gap in the line, not a plotted point. Replace zero values with `null` so the charting library renders a break in the line instead of dropping to the floor.

```ts
// When building chart data points:
const safeScore = (score === 0 || score == null) ? null : score;
```

If the chart library requires a specific "no data" sentinel, use whatever that library expects — the goal is no line drawn through zero-value points.

---

**2. Add performance band regions behind the chart line**

Behind the score line, render three horizontal filled regions across the full width of the chart:

- DEPLETED: y = 0–49 — fill with `rgba(255, 45, 85, 0.08)` 
- BALANCED: y = 50–74 — fill with `rgba(0, 229, 255, 0.06)`
- PEAK: y = 75–100 — fill with `rgba(180, 255, 80, 0.08)`

These should render behind the line, not on top of it. Add small text labels on the right edge of each band ("DEPLETED", "BALANCED", "PEAK") at 9px, muted color, so the user understands what zone their line is living in.

---

**3. Add a dotted goal line**

Draw a horizontal dotted line across the chart at y = 75 (the target average score). Style it as:
- 1px wide
- Dotted/dashed
- Color: `rgba(255, 255, 255, 0.2)`

Add a small label "GOAL" at the right end of this line in the same muted style as the band labels.

---

**4. Reframe the stat strip**

The current stat strip shows MIN / AVG / MAX / N. Replace it with:

| Label | Value | Source |
|---|---|---|
| AVG SCORE | existing avg score value | same as before |
| TREND | ↑ or ↓ or — | compare first half of period avg vs second half avg; if difference > 3 show ↑ or ↓, otherwise — |
| COMPLIANCE | existing `weeklyCompliancePct` value + "%" | already calculated in the protocol engine |
| STREAK | existing `complianceStreak` value + "d" | already in userState |

Remove MIN, MAX, and N (snapshot count) from the strip entirely. These are demoralizing for new users with sparse data.

---

**5. Hide empty day cards**

In the list of daily cards below the chart, do not render a day card if that day has zero intakes AND zero snapshots. A day with no data should not appear in the list at all. If the entire period has no data, show the existing empty state component.

If a day has snapshots but zero intakes, render the card normally — the score data is still useful.

---

**6. Restructure the day card content**

Replace the current day card layout with this structure:

```
[Date]                    [Avg score] [trend arrow]
[N snapshots · N intakes]
─────────────────────────────────────────────────
[Total oz] consumed  ·  [AForce units] units  ·  [compliance]% protocol

Sodium balance     [sodium in] mg in · [sodium lost] mg lost · [net] mg net
Best window        [time range when score was highest that day]
Today's lesson     [one-line coaching string — see below]
```

**Best window logic:** Find the snapshot with the highest score for that day. Report the 2-hour window centered on that snapshot's timestamp (e.g. if best snapshot was at 2:30 PM, report "1:30 PM – 3:30 PM"). If only one snapshot exists for the day, report a 1-hour window centered on it. If no snapshot has a score above 50, omit this row entirely.

**Today's lesson logic:** Pick one string based on the day's data:
- If avg score < 50: `"Increase fluid intake — aim for your daily oz target before 6 PM"`
- If avg score 50–74 and compliance < 80%: `"Tighten your intake timing to hold the Balanced window longer"`
- If avg score 50–74 and compliance >= 80%: `"You're consistent — push intake volume to break into Peak"`
- If avg score >= 75: `"Strong day. Maintain this intake pattern tomorrow"`
- If zero intakes: `"Log your next intake to activate protocol tracking"`

**Remove** "Time per band" and the band percentage tags (e.g. "Depleted 100%") from the day card entirely. Replace with the above structure.

**Keep** the Autopilot and Social mode tags at the bottom of the card — those are useful context.

---

**7. Seed a baseline snapshot on first Confirm Status**

In the store action that handles `confirmStatus`, check if this is the user's first-ever confirmation (i.e. history array is currently empty). If so, also write a synthetic "Day 0 baseline" entry into the journal history for yesterday's date with:
- Score: same as the current score
- Label: `"Baseline — first check-in"`
- Intakes: 0
- A flag `isSynthetic: true` so it can be filtered out of export PDFs

This gives the chart at least two points on day one so it renders a line rather than a single dot.

---

**8. PDF export — enrich the content**

In the PDF export logic (wherever `Export PDF` is implemented), ensure the exported document includes:

- The trend chart for the selected period (7d / 30d / 90d)
- Weekly compliance percentage
- Average score for the period
- Streak length
- Sodium balance summary (total in vs total lost vs net)
- Protocol stage history (list of stages the user was in during the period with dates)
- A footer line: `"Generated by AForce OS · aforce.os"`

Do not include the `isSynthetic: true` baseline entries in the PDF export.

---

**Do not:**
- Change any other tab screens
- Modify the scoring engine
- Alter navigation or route structure
- Touch the chart library configuration beyond what's needed for bands, goal line, and null gaps
- Change the 7d / 30d / 90d toggle behavior

**After all changes**, verify TypeScript compiles cleanly with `npx tsc --noEmit` and that no imports are broken. The journal screen should render without errors in all three time range modes (7d, 30d, 90d) including when the user has no data at all.

---

That covers every fix. The most impactful ones are 1 (zero floor), 2 (band regions), and 6 (day card restructure) — if Replit runs short on context, prioritize those three first.