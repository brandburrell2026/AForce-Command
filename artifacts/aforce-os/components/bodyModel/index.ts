/**
 * Shared body-model input components. One height/weight contract for
 * every surface (onboarding, Edit Profile, Sweat Calculator) — see
 * `utils/bodyMeasurements.ts` for the pure logic they wrap.
 */
export { HeightField } from './HeightField';
export { WeightField } from './WeightField';
