/**
 * Compare route — thin wrapper around screens/ComparisonScreen.
 * Keeps the file structure spec ("/screens/ComparisonScreen.tsx") intact
 * while satisfying expo-router's app-directory routing.
 */
export { default } from '@/screens/ComparisonScreen';
