# CALENDAR

Monthly master calendars live here as `YYYY-MM-content-calendar.csv` (fields defined in `../agents/09-calendar-manager.md`).

**Campaign takeover months:** the campaign's own calendar is authoritative and the master defers to it.
**September 2026 (current):** the launch owns the month — the authoritative calendar is
`../campaigns/hydration-sticks-launch/calendar.csv` (Sep 1–30, "The Ritual Travels").

Posting-time defaults are HYPOTHESES until `../learning/content_insights.md` has real timing data.
